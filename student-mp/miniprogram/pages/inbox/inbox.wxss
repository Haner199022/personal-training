.top { margin-bottom: 24rpx; }
.msg { margin-bottom: 16rpx; }
.is-read { opacity: 0.6; }
.msg-h { display: flex; align-items: center; }
.dot { color: var(--c-brand); font-size: 24rpx; margin-right: 12rpx; }
.title { font-weight: 600; color: var(--c-text); flex: 1; }
.read-tag { font-size: 22rpx; color: var(--c-mute); margin-left: 12rpx; }
.sub { margin-top: 8rpx; }
.empty { text-align: center; margin-top: 120rpx; }
